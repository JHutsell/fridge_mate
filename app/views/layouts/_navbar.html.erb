<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <li class="navbar-brand">
        <% if current_user != nil %>
            <%= link_to "Fridge Mate", user_path(current_user) %>
        <% else %>
            <%= link_to "Fridge Mate", welcome_path %>
        <% end %>
    </li>
    <div id="identity-actions" class="collapse navbar-collapse">
        <ul class="navbar-nav ml-auto">
            <% if !logged_in? %>
                <li><%= button_to "Login", '/login', method: :get %><br></li>
                <li><%= button_to "Sign Up", '/users/new', method: :get %></li>
            <% else %>
                <li class="navbar-text">Logged In as <%= current_user.username %></li>
                <li><%= button_to "Logout", '/logout', method: :delete %></li>
            <% end %>
        </ul>
    </div>
</nav>